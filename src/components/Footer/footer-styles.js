import styled from 'styled-components';

   export const FooterWrapper = styled.div`
   text-decoration: none;
   padding-left: 5%;
   padding-right: 5%;
   `

   export const FooterCol1 = styled.div`
   
   `

   export const FooterCol3 = styled.div`
   
   `

   export const FooterCol4 = styled.div`
   
   `